{
  "name": "no-man-band",
  "version": "1.0.0",
  "description": "Generative MIDI music app",
  "main": "server/server.js",
  "scripts": {
    "repl": "node noManBand.js",
    "server": "node server/server.js",
    "client": "npm --prefix client run dev",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "build": "npm --prefix client run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@julusian/midi": "^3.6.1",
    "concurrently": "^9.1.0",
    "express": "^5.1.0",
    "lodash": "^4.17.20",
    "socket.io": "^4.8.0"
  }
}
